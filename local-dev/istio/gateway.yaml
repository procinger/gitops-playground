apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: tls-gateway
  namespace: istio-gateway
spec:
  selector:
    istio: gateway
  servers:
    - port:
        name: http
        number: 80
        protocol: HTTP
      hosts:
        - '*.local'

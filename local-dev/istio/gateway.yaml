apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: kubedev-net
  namespace: istio-gateway
spec:
  selector:
    istio: gateway
  servers:
    - port:
        name: http
        number: 80
        protocol: HTTP
      hosts:
        - '*.local'
